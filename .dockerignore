# Keep the Docker build context small and reproducible: the docs image only serves the pre-generated
# HtmlDocs/index.html, so build artifacts and VCS metadata are not needed.
**/bin/
**/obj/
.git/
.github/
.vs/
report-output/
*.user
